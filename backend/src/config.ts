const config = {
  port: 3021,
  nodeEnv: "development" as "development" | "production" | "test",
  dbPath: "./data/infinite-adventures.db",
  supabaseUrl: "",
  supabaseServiceRoleKey: "",
  isProduction: false,
  isDevelopment: true,
};

export default config;
