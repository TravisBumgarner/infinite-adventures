{
  "name": "infinite-adventures",
  "private": true,
  "workspaces": [
    "packages/frontend",
    "packages/backend",
    "packages/shared",
    "packages/infra",
    "packages/desktop"
  ],
  "scripts": {
    "dev": "concurrently -n docker,backend,frontend -c gray,blue,green \"docker compose up\" \"npm --prefix packages/backend run dev\" \"npm --prefix packages/frontend run dev\"",
    "test": "npm --prefix packages/frontend test && npm --prefix packages/backend test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "build:heroku": "npm --prefix packages/frontend run build && npm --prefix packages/backend run build",
    "heroku-postbuild": "npm run build:heroku",
    "deploy:heroku:main": "bash scripts/deploy-heroku.sh main",
    "deploy:heroku:branch": "bash scripts/deploy-heroku.sh HEAD",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.13",
    "concurrently": "^9.2.1",
    "husky": "^9.1.7"
  }
}
