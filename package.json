{
  "name": "infinite-adventures",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared",
    "infra"
  ],
  "scripts": {
    "dev": "concurrently -n docker,backend,frontend -c gray,blue,green \"docker compose up\" \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "test": "npm --prefix frontend test && npm --prefix backend test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "build:heroku": "npm --prefix frontend run build && npm --prefix backend run build",
    "heroku-postbuild": "npm run build:heroku",
    "deploy:heroku:main": "bash scripts/deploy-heroku.sh main",
    "deploy:heroku:branch": "bash scripts/deploy-heroku.sh HEAD",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.13",
    "concurrently": "^9.2.1",
    "husky": "^9.1.7"
  }
}
