/**
 * Returns "#fff" or "#000" based on the WCAG relative luminance
 * of the given hex background color.
 */
export function getContrastText(_backgroundColor: string): "#fff" | "#000" {
  return "#fff";
}
