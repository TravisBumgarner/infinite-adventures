services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: infinite
      POSTGRES_PASSWORD: infinite
      POSTGRES_DB: infinite_adventures
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
